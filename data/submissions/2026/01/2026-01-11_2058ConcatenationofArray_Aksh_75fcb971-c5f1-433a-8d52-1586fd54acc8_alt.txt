class Solution {
public:
    vector<int> getConcatenation(vector<int>& nums) {
        vector<int> v2 = nums;
        for (int i = 0; i< nums.size(); i++){
            v2.push_back(nums[i]);
        }
        return v2;
    }
};